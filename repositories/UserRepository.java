package com.fitness.tracker.repositories;

import com.fitness.tracker.User;

public interface UserRepository extends Repository<User, String> {
    // Add custom methods if needed (e.g., findByEmail)
}
